@model Ekbatan.DomainClasses.MelkPosition.MelkPosition

@{
    ViewData["Title"] = "افزودن";
    Layout = "~/Views/Shared/_DefaultLayout.cshtml";
}

<h4 class="btn btn-primary"><i class="fa fa-plus"></i>افزایش موقعیت ملک</h4>

<hr />
<div class="row">
    <div class="col-md-8">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="MelkPosition_Desc" class="control-label"></label>
                <input asp-for="MelkPosition_Desc" class="form-control" />
                <span asp-validation-for="MelkPosition_Desc" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="ثبت" class="btn btn-danger" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-info"> بازگشت به لیست نوع موقعیت املاک ثبت شده </a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
