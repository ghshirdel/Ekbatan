@model IEnumerable<Ekbatan.DomainClasses.MelkPosition.MelkPosition>

@{
    ViewData["Title"] = "فهرست";
    Layout = "~/Views/Shared/_DefaultLayout.cshtml";
}

<h1><i class="fa fa-list-ul"></i> @ViewData["Title"] </h1>
<br />
<p>
    <a asp-action="Create" class="btn btn-primary"> <i class="fa fa-plus"></i> افزودن به لیست موقعیت ملک</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MelkPosition_Desc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MelkPosition_Desc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MelkPosition_ID" class="btn btn-info"><i class="fa fa-edit"></i>ویرایش</a> |
                <a asp-action="Details" asp-route-id="@item.MelkPosition_ID" class="btn btn-success"><i class="fa fa-info-circle"></i>جزئیات</a> |
                <a asp-action="Delete" asp-route-id="@item.MelkPosition_ID" class="btn btn-danger"><i class="fa fa-trash"></i>حذف</a>
            </td>
        </tr>
}
    </tbody>
</table>
