@model IEnumerable<Ekbatan.DomainClasses.Karbari.Karbari>

@{
    ViewData["Title"] = "فهرست کاربری املاک ";
    Layout = "~/Views/Shared/_DefaultLayout.cshtml";
}

<h2><i class="fa fa-list-ul"></i>@ViewData["Title"]</h2>
<br />
<p>
    <a asp-action="Create" class="btn btn-primary"> <i class="fa fa-plus"></i> افزودن به لیست کاربری املاک</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Karbari_Desc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Karbari_Desc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Karbari_ID" class="btn btn-info"><i class="fas fa-edit"></i>ویرایش</a> |
                <a asp-action="Details" asp-route-id="@item.Karbari_ID" class="btn btn-success"><i class="fas fa-info-circle"></i>جزئیات</a> |
                <a asp-action="Delete" asp-route-id="@item.Karbari_ID" class="btn btn-danger"><i class="fas fa-trash"></i>حذف</a>
            </td>
        </tr>
}
    </tbody>
</table>
