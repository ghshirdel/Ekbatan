@model Ekbatan.DomainClasses.FrontAge.FrontAge

@{
    ViewData["Title"] = "افزودن";
    Layout = "~/Views/Shared/_DefaultLayout.cshtml";
}


<h4 class="btn btn-primary"> <i class="fa fa-edit"></i> افزودن نوع نمای ساختمان</h4>
<hr />
<div class="row">
    <div class="col-md-8">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FrontAge_Desc" class="control-label"></label>
                <input asp-for="FrontAge_Desc" class="form-control" />
                <span asp-validation-for="FrontAge_Desc" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="ثبت" class="btn btn-danger" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-info">بازگشت به لیست نماهای ساختمان ثبت شده</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
